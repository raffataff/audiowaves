{
  "version": "1.0",
  "exportedAt": "2026-04-15T01:58:52.778Z",
  "shaderCount": 19,
  "shaders": [
    {
      "index": 0,
      "name": "Playa",
      "thumbnail": "data:image/jpeg;base64,",
      "params": {
        "speed": -0.7,
        "intensity": 0.7,
        "colorShift": 2,
        "amplitude": 0.8
      },
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform float u_beat;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\nuniform float u_speed;\nuniform float u_intensity;\nuniform float u_colorShift;\nuniform float u_amplitude;\n\n// Defaults for missing sliders\n#define u_glow 1.0\n#define u_complexity 1.0\n#define u_distortion 0.0\n#define u_rotation 0.0\n#define u_scale 1.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            return texture(u_spectrum, vec2(clamp(f, 0.01, 0.99), 0.0)).r;\n        }\n        \n\n                vec3 palette(float t) {\n                    return mix(vec3(1.0, 0.0, 0.0), vec3(1.0, 1.0, 0.0), t);\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, 4);\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    \n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    // --- Layer 1: Lightning & Electric ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.72;\n        \n            vec2 p = layerUV * (2.0 * u_colorShift);\n            p += vec2(noise(p + localTime), noise(p - localTime)) * u_mid;\n            float val = 0.0;\n            float amp = 1.0;\n            for(int i=0; i<5; i++) {\n                val += abs(sin(p.x * 5.0 + p.y * (2.0 * u_colorShift) + localTime * 2.0 + noise(p + localTime))) * amp;\n                p *= 2.0;\n                amp *= (0.5 * (0.8 + u_mid * 0.5));\n            }\n            float electricity = 0.05 / (val * val + (0.001 * u_amplitude));\n            vec3 col = mix(vec3(0.5, 0.1, (0.8 * (0.8 + u_mid * 0.5))), vec3(0.2, (0.8 * (0.8 + u_mid * 0.5)), 1.0), u_mid);\n            vec3 layerColor = col * electricity * (1.0 + getFreq(0.5) * 3.0);\n            \n        layerColor *= u_amplitude * (0.8 + u_mid * 0.4);\n        finalColor = layerColor;\n    }\n\n    // --- Layer 2: Bio-Digital (Xor) ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.5) * 0.1 * (1.0 + u_bass);\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.71;\n        \n            vec3 p = vec3(0.0);\n            vec3 v = vec3(1.0, 2.0, 6.0);\n            vec3 col = vec3(0.0);\n            vec3 rayDir = normalize(vec3(layerUV, -1.0));\n            float z = 0.0;\n            float d = 0.0;\n            float f = 0.0;\n            \n            float t = localTime * 2.0 + u_bass * 1.50;\n\n            for(float i=0.0; i<50.0; i++) {\n                p = z * rayDir;\n                float rotArg = (p.y + sin(p.y)) * 0.4;\n                float c = cos(rotArg);\n                float s = sin(rotArg);\n                p.xz *= mat2(c, -s, s, c);\n                p.x += t;\n                \n                vec3 distVec = cos(p / v) * v + vec3(v.z, v.x, v.x) / 7.0;\n                d = length(distVec);\n                \n                f = 2.0 + d / exp(p.y * (0.5 * u_bass));\n                z += d / f;\n                \n                vec3 pal = cos((vec3(p.x) + z + v) * 0.1) + (1.0 + u_bass * 0.2);\n                // SAFE: Ensure max is at least 0.001 regardless of bass\n                col += pal / max(0.001, d * f * z);\n            }\n            \n            layerColor = tanh(col * 0.1 + u_bass * 0.1);\n            \n        layerColor *= u_amplitude * (0.8 + u_bass * 0.4);\n        finalColor = blend_Difference(finalColor, layerColor, 0.5 + u_bass * 0.2);\n    }\n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 + u_time) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = 0.95; \n    #ifdef u_decay\n        dcAmount = u_decay;\n    #endif\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = 0.0;\n    #ifdef u_feedback\n        fbAmount = u_feedback;\n    #endif\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.7 || u_beat > 0.9) {\n        // Force feedback ON during the beat\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.777Z"
    },
    {
      "index": 1,
      "name": "Mowgli",
      "thumbnail": "data:image/jpeg;base64,",
      "params": {
        "speed": 1,
        "intensity": 1.4,
        "scale": 1.21,
        "colorShift": 1.3
      },
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform float u_beat;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\nuniform float u_speed;\nuniform float u_intensity;\nuniform float u_scale;\nuniform float u_colorShift;\n\n// Defaults for missing sliders\n#define u_amplitude 1.0\n#define u_glow 1.0\n#define u_complexity 1.0\n#define u_distortion 0.0\n#define u_rotation 0.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            return texture(u_spectrum, vec2(clamp(f, 0.01, 0.99), 0.0)).r;\n        }\n        \n\n                vec3 palette(float t) {\n                    return 0.5 + 0.5 * cos(6.28318 * (t + vec3(0.0, 0.33, 0.67)));\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, 4);\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    uv /= (u_scale * 0.5 + 0.1 + u_bass * 0.05);\n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    // --- Layer 1: Spectrum Analyzer ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.54;\n        \n            float dist = length(layerUV);\n            float angle = atan(layerUV.y, layerUV.x);\n            float fIndex = abs(angle) / 3.14159; \n            float val = getFreq(fIndex);\n            float ring = smoothstep(0.01, 0.0, abs(dist - ((0.2 * (0.8 + u_treble * 0.5)) + val * (0.5 * (0.8 + u_treble * 0.5)))));\n            ring += smoothstep(0.2, 0.0, abs(dist - (0.2 + val * 0.5))) * (0.5 * u_intensity) * u_treble;\n            vec3 layerColor = palette(fIndex + localTime * 0.2) * ring;\n            \n        layerColor *= u_amplitude * (0.8 + u_treble * 0.4);\n        finalColor = layerColor;\n    }\n\n    // --- Layer 2: Cosmic Space ---\n    {\n        currentUV = initialUV;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.49;\n        \n            vec2 p = layerUV;\n            float r = length(p);\n            float a = atan(p.y, p.x);\n            float arm = sin(a * 3.0 + r * 10.0 - localTime);\n            float gal = exp(-r * (2.0 * (0.8 + u_beat * 0.5))) * (1.0 + arm * (0.5 * u_scale));\n            vec3 layerColor = palette(r + localTime*(0.1 * (0.8 + u_beat * 0.5))) * gal * (1.0+u_beat);\n            \n        layerColor *= u_amplitude * (0.8 + u_beat * 0.4);\n        finalColor = blend_Exclusion(finalColor, layerColor, 0.5 + u_beat * 0.2);\n    }\n\n    // --- Layer 3: Plasma Waves ---\n    {\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.80;\n        \n            float n = fbm(layerUV * (3.0 * (0.8 + u_beat * 0.5)) + localTime * 0.1, 4);\n            // SAFE: Dist never 0\n            float dist = length(layerUV) + (0.1 * u_intensity);\n            float core = 1.0 / dist;\n            vec3 layerColor = palette(n * 2.0) * n * core * 0.5 * ((0.8 * u_scale) + u_beat * 0.5);\n            \n        layerColor *= u_amplitude * (0.8 + u_beat * 0.4);\n        finalColor = blend_Exclusion(finalColor, layerColor, 0.5 + u_beat * 0.2);\n    }\n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 + u_time) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = 0.95; \n    #ifdef u_decay\n        dcAmount = u_decay;\n    #endif\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = 0.0;\n    #ifdef u_feedback\n        fbAmount = u_feedback;\n    #endif\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.7 || u_beat > 0.9) {\n        // Force feedback ON during the beat\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.778Z"
    },
    {
      "index": 2,
      "name": "Shimmer",
      "thumbnail": "data:image/jpeg;base64,",
      "params": {
        "speed": -0.5,
        "intensity": 1.5,
        "colorShift": 1.1,
        "complexity": 0.6,
        "amplitude": 3
      },
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform float u_beat;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\nuniform float u_speed;\nuniform float u_intensity;\nuniform float u_colorShift;\nuniform float u_complexity;\nuniform float u_amplitude;\n\n// Defaults for missing sliders\n#define u_glow 1.0\n#define u_distortion 0.0\n#define u_rotation 0.0\n#define u_scale 1.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            return texture(u_spectrum, vec2(clamp(f, 0.01, 0.99), 0.0)).r;\n        }\n        \n\n                vec3 palette(float t) {\n                    return mix(vec3(1.0, 0.0, 0.0), vec3(1.0, 1.0, 0.0), t);\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, int(u_complexity * 4.0 + 2.0));\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    \n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    // --- Layer 1: Spectrum Analyzer ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.0;\n        \n            float val = getFreq(abs(layerUV.x));\n            float wave = sin(layerUV.x * (10.0 * u_complexity) + localTime * (2.0 * u_intensity)) * (0.1 * (0.8 + u_treble * 0.5));\n            float line = smoothstep(0.01, 0.009, abs(layerUV.y - (val - 0.5 + wave)));\n            float fill = smoothstep(0.0, 0.9, (val - 0.5 + wave) - layerUV.y);\n            vec3 layerColor = palette(layerUV.x * (2.0 * u_complexity) + localTime) * (line + fill * 0.5);\n            layerColor += exp(-abs(layerUV.y) * (2.0 * (0.8 + u_treble * 0.5))) * u_treble * vec3(0.5, (0.2 * (0.8 + u_treble * 0.5)), 1.0);\n            \n        layerColor *= u_amplitude * (0.5 + u_treble * 0.4);\n        finalColor = layerColor;\n    }\n\n    \n    // --- Layer 2: Fractal Patterns ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.5) * 0.1 * (1.0 + u_bass);\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.67;\n        \n            vec2 p = layerUV;\n            p = p / (u_scale + 0.1) - vec2((0.7 * (0.8 + u_treble * 0.5)), 0.0); \n            vec2 c = p;\n            vec2 z = vec2(0.0);\n            float iter = 0.0;\n            for(int i=0; i < int(float(8) * u_complexity); i++) {\n                z = vec2(z.x*z.x - z.y*z.y, (2.0 * u_amplitude)*z.x*z.y) + c;\n                if(length(z) > (2.0 * (0.1 + u_treble * 0.5))) break;\n                iter += 1.0;\n            }\n            vec3 layerColor = palette(iter/8.0 + localTime * (0.2 * u_amplitude)) * u_beat;\n            \n        layerColor *= u_amplitude * (0.8 + u_treble * 0.4);\n        finalColor = blend_Difference(finalColor, layerColor, 0.5 + u_treble * 0.2);\n    }\n\n\n    \n    // --- Layer 3: Wave Patterns ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.5) * 0.1 * (0.10 + u_mid);\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.86;\n        \n             float d = length(layerUV);\n             float ripple = sin(d * 30.0 - localTime * 5.0);\n             vec3 layerColor = vec3(0.0, 0.5, 1.0) * (ripple * 0.5 + (0.5 * (0.8 + u_bass * 0.5))) * u_beat;\n             \n        layerColor *= u_amplitude * (0.8 + u_beat * 0.4);\n        finalColor = blend_SoftLight(finalColor, layerColor, 0.5 + u_beat * 0.2);\n    }\n\n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 + u_time) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = 0.95; \n    #ifdef u_decay\n        dcAmount = u_decay;\n    #endif\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = 0.0;\n    #ifdef u_feedback\n        fbAmount = u_feedback;\n    #endif\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.7 || u_beat > 0.9) {\n        // Force feedback ON during the beat\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.778Z"
    },
    {
      "index": 3,
      "name": "sda",
      "thumbnail": "data:image/jpeg;base64,",
      "params": {
        "speed": 0.1,
        "intensity": 0.6,
        "scale": 1.11,
        "colorShift": 4.8,
        "complexity": 1.9
      },
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform float u_beat;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\nuniform float u_speed;\nuniform float u_intensity;\nuniform float u_scale;\nuniform float u_colorShift;\nuniform float u_complexity;\n\n// Defaults for missing sliders\n#define u_amplitude 1.0\n#define u_glow 1.0\n#define u_distortion 0.0\n#define u_rotation 0.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n#define u_feedback 0.0\n#define u_decay 0.95\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            return texture(u_spectrum, vec2(clamp(f, 0.01, 0.99), 0.0)).r;\n        }\n        \n\n                vec3 palette(float t) {\n                    return 0.5 + 0.5 * cos(6.28318 * (t + vec3(0.0, 0.33, 0.67)));\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, int(u_complexity * 4.0 + 2.0));\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    uv /= (u_scale * 0.5 + 0.1 + u_bass * 0.05);\n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    // --- Layer 1: Tunnel Effect ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.73;\n        \n            float r = 1.0/length(layerUV) + localTime;\n            float a = atan(layerUV.y, layerUV.x);\n            float v = sin(r * (10.0 * u_intensity) + u_beat) * cos(a * 8.0);\n            vec3 layerColor = palette(v * 0.5 + 0.5);\n             \n        layerColor *= u_amplitude * (0.8 + u_beat * 0.4);\n        finalColor = layerColor;\n    }\n\n    // --- Layer 2: Fractal Patterns ---\n    {\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.74;\n        \n            vec2 p = layerUV * 1.5;\n            vec2 c = vec2(sin(localTime*0.3), cos(localTime*0.4));\n            float i_val = 0.0;\n            for(int i=0; i < int(float(6) * u_complexity); i++) {\n                p = vec2(p.x*p.x - p.y*p.y, (2.0 * (0.8 + u_beat * 0.5))*p.x*p.y) + c;\n                if(length(p) > 4.0) break;\n                i_val += 1.0;\n            }\n            vec3 layerColor = palette(i_val * (0.1 * u_scale) + localTime);\n            \n        layerColor *= u_amplitude * (0.8 + u_beat * 0.4);\n        finalColor = blend_Add(finalColor, layerColor, 0.5 + u_beat * 0.2);\n    }\n\n    // --- Layer 3: Crystal Structures ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.5) * 0.1 * (1.0 + u_bass);\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.88;\n        \n            vec2 p = layerUV * 3.0;\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            float n = random(i); \n            float glass = smoothstep(0.0, (0.1 * u_colorShift), abs(f.x - f.y + n - 0.5));\n            vec3 layerColor = vec3((0.8 * u_intensity), 0.9, 1.0) * glass * (0.5 + (0.5 * u_complexity) * sin(n * 10.0 + localTime));\n            \n        layerColor *= u_amplitude * (0.8 + u_bass * 0.4);\n        finalColor = blend_Screen(finalColor, layerColor, 0.5 + u_bass * 0.2);\n    }\n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 + u_time) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = 0.95; \n    #ifdef u_decay\n        dcAmount = u_decay;\n    #endif\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = 0.0;\n    #ifdef u_feedback\n        fbAmount = u_feedback;\n    #endif\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.7 || u_beat > 0.9) {\n        // Force feedback ON during the beat\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.778Z"
    },
    {
      "index": 4,
      "name": "OBE",
      "thumbnail": "data:image/jpeg;base64,",
      "params": {
        "speed": -0.3,
        "intensity": 0.8,
        "scale": 1.21,
        "colorShift": 3
      },
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform float u_beat;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\nuniform float u_speed;\nuniform float u_intensity;\nuniform float u_scale;\nuniform float u_colorShift;\n\n// Defaults for missing sliders\n#define u_amplitude 1.0\n#define u_glow 1.0\n#define u_complexity 1.0\n#define u_distortion 0.0\n#define u_rotation 0.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n#define u_feedback 0.0\n#define u_decay 0.95\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            return texture(u_spectrum, vec2(clamp(f, 0.01, 0.99), 0.0)).r;\n        }\n        \n\n                vec3 palette(float t) {\n                    return 0.5 + 0.5 * cos(6.28318 * (t + vec3(0.0, 0.33, 0.67)));\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, 4);\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    uv /= (u_scale * 0.5 + 0.1 + u_bass * 0.05);\n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    // --- Layer 1: Fluid Dynamics ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.57;\n        \n            float d = length(layerUV);\n            float u = noise(layerUV * 5.0 + localTime);\n            float v = noise(layerUV * 10.0 - localTime);\n            float f = 0.5 + (0.5 * (0.8 + u_bass * 0.5)) * sin(10.0 * (u - v));\n            vec3 layerColor = mix(vec3(0.1, 0.0, 0.2), vec3(0.0, 1.0, 0.8), f) * (1.0-d);\n            \n        layerColor *= u_amplitude * (0.8 + u_bass * 0.4);\n        finalColor = layerColor;\n    }\n\n    // --- Layer 2: Lightning & Electric ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.5) * 0.1 * (1.0 + u_bass);\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.96;\n        \n            vec2 p = layerUV * 2.0;\n            p += vec2(noise(p + localTime), noise(p - localTime)) * u_treble;\n            float val = 0.0;\n            float amp = 1.0;\n            for(int i=0; i<5; i++) {\n                val += abs(sin(p.x * 5.0 + p.y * (2.0 * u_scale) + localTime * 2.0 + noise(p + localTime))) * amp;\n                p *= 2.0;\n                amp *= (0.5 * (0.8 + u_treble * 0.5));\n            }\n            float electricity = (0.05 * (0.8 + u_treble * 0.5)) / (val * val + 0.001);\n            vec3 col = mix(vec3(0.5, 0.1, 0.8), vec3(0.2, 0.8, 1.0), u_treble);\n            vec3 layerColor = col * electricity * (1.0 + getFreq(0.5) * (3.0 * (0.8 + u_treble * 0.5)));\n            \n        layerColor *= u_amplitude * (0.8 + u_treble * 0.4);\n        finalColor = blend_Overlay(finalColor, layerColor, 0.5 + u_treble * 0.2);\n    }\n\n    // --- Layer 3: Bio-Digital (Xor) ---\n    {\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.58;\n        \n            vec3 rd = normalize(vec3(layerUV, 1.0));\n            vec3 p = vec3(0.0);\n            float t = 0.0;\n            vec3 acc = vec3(0.0);\n            for(int i=0; i<32; i++) {\n                p = rd * t;\n                p.z += localTime + u_mid; \n                p.xy = rotate(p.xy, localTime * 0.2);\n                float d = dot(sin(p), cos(p.yzx)) / 1.5;\n                d = abs(d) - 0.1;\n                if(d < 0.01) {\n                    acc += palette(t * 0.1 + u_mid) * 0.1;\n                }\n                t += max(0.05, d * 0.5);\n            }\n            float intens = 1.0;\n            #ifdef u_intensity\n                intens = u_intensity;\n            #endif\n            vec3 layerColor = acc * intens;\n            \n        layerColor *= u_amplitude * (0.8 + u_mid * 0.4);\n        finalColor = blend_SoftLight(finalColor, layerColor, 0.5 + u_mid * 0.2);\n    }\n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 + u_time) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = 0.95; \n    #ifdef u_decay\n        dcAmount = u_decay;\n    #endif\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = 0.0;\n    #ifdef u_feedback\n        fbAmount = u_feedback;\n    #endif\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.7 || u_beat > 0.9) {\n        // Force feedback ON during the beat\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.778Z"
    },
    {
      "index": 5,
      "name": "Daytona",
      "thumbnail": "data:image/jpeg;base64,",
      "params": {},
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform float u_beat;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\n\n\n// Defaults for missing sliders\n#define u_speed 1.0\n#define u_intensity 1.0\n#define u_amplitude 1.0\n#define u_glow 1.0\n#define u_complexity 1.0\n#define u_colorShift 0.0\n#define u_distortion 0.0\n#define u_rotation 0.0\n#define u_scale 1.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n#define u_feedback 0.0\n#define u_decay 0.95\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            return texture(u_spectrum, vec2(clamp(f, 0.01, 0.99), 0.0)).r;\n        }\n        \n\n                vec3 palette(float t) {\n                    return 0.5 + 0.5 * cos(6.28318 * (t + vec3(0.0, 0.33, 0.67)));\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, 4);\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    \n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    \n\n    // --- Layer 2: Fractal Patterns ---\n    {\n        currentUV = initialUV;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * 1.20;\n        \n            vec2 p = layerUV * 20.0;\n            vec3 col = vec3(0.0);\n            float t = localTime + u_treble * 2.0;\n            for(float i=0.0; i<10.0; i++) {\n                vec3 pal = cos(p.x + vec3(2.0, 1.0, 0.0)) + 1.0;\n                vec2 distortion = sin(p + t).yx;\n                float d = length(sin(p + distortion + u_treble * 0.3));\n                col += pal / max(0.001, d - u_treble * 0.15) / 0.2;\n                p *= mat2(0.8, -0.6, 0.6, 0.8);\n            }\n            vec3 layerColor = tanh(col * col / 20000.0);\n            \n        layerColor *= u_amplitude * (0.8 + u_treble * 0.4);\n        finalColor = blend_Difference(finalColor, layerColor, 0.5 + u_treble * 0.2);\n    }\n\n    // --- Layer 3: Spectrum Analyzer ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g)) * 0.01;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * 1.81;\n        \n            float dist = length(layerUV);\n            float angle = atan(layerUV.y, layerUV.x);\n            float fIndex = abs(angle) / 3.14159; \n            float val = getFreq(fIndex);\n            float ring = smoothstep(0.01, 0.0, abs(dist - (0.2 + val * 0.5)));\n            ring += smoothstep(0.2, 0.0, abs(dist - (0.2 + val * 0.5))) * 0.5 * u_mid;\n            vec3 layerColor = palette(fIndex + localTime * 0.2) * ring;\n            \n        layerColor *= u_amplitude * (0.8 + u_mid * 0.4);\n        finalColor = blend_Add(finalColor, layerColor, 0.5 + u_mid * 0.2);\n    }\n\n    // --- Layer 1: Bio-Digital (Xor) ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g)) * 0.01 ;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * 0.71;\n        \n            vec3 p = vec3(0.0);\n            vec3 v = vec3(1.0, 2.0, 6.0);\n            vec3 col = vec3(0.0);\n            vec3 rayDir = normalize(vec3(layerUV, -1.0));\n            float z = 0.0;\n            float d = 0.0;\n            float f = 0.0;\n            \n            float t = localTime * 2.0 + u_bass * 1.50;\n\n            for(float i=0.0; i<50.0; i++) {\n                p = z * rayDir;\n                float rotArg = (p.y + sin(p.y)) * 0.4;\n                float c = cos(rotArg);\n                float s = sin(rotArg);\n                p.xz *= mat2(c, -s, s, c);\n                p.x += t;\n                \n                vec3 distVec = cos(p / v) * v + vec3(v.z, v.x, v.x) / 7.0;\n                d = length(distVec);\n                \n                f = 2.0 + d / exp(p.y * (0.5 * u_bass));\n                z += d / f;\n                \n                vec3 pal = cos((vec3(p.x) + z + v) * 0.1) + (1.0 + u_bass * 0.2);\n                // SAFE: Ensure max is at least 0.001 regardless of bass\n                col += pal / max(0.001, d * f * z);\n            }\n            \n            layerColor = tanh(col * 0.1 + u_bass * 0.1);\n            \n        layerColor *= u_amplitude * (0.8 + u_bass * 0.4);\n        finalColor = blend_Difference(finalColor, layerColor, 0.5 + u_treble * 0.2);\n    }\n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 + u_time) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = 0.95; \n    #ifdef u_decay\n        dcAmount = u_decay;\n    #endif\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = 0.0;\n    #ifdef u_feedback\n        fbAmount = u_feedback;\n    #endif\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.7 || u_beat > 0.9) {\n        // Force feedback ON during the beat\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.778Z"
    },
    {
      "index": 6,
      "name": "953",
      "thumbnail": "data:image/jpeg;base64,",
      "params": {
        "speed": 1,
        "intensity": 1.3,
        "scale": 1.21,
        "distortion": 0.6,
        "amplitude": 1.3
      },
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform float u_beat;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\nuniform float u_speed;\nuniform float u_intensity;\nuniform float u_scale;\nuniform float u_distortion;\nuniform float u_amplitude;\n\n// Defaults for missing sliders\n#define u_glow 1.0\n#define u_complexity 1.0\n#define u_colorShift 0.0\n#define u_rotation 0.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n#define u_feedback 0.0\n#define u_decay 0.95\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            return texture(u_spectrum, vec2(clamp(f, 0.01, 0.99), 0.0)).r;\n        }\n        \n\n                vec3 palette(float t) {\n                    return mix(vec3(0.0, 0.3, 0.8), vec3(0.0, 0.8, 0.6), t);\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, 4);\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    uv /= (u_scale * 0.5 + 0.1 + u_bass * 0.05);\n    \n                uv += (vec2(\n                    noise(uv * 2.0 + u_time * 0.2),\n                    noise(uv * 2.0 - u_time * 0.2)\n                ) - 0.5) * u_distortion * 0.5;\n            \n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    // --- Layer 1: Plasma Waves ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.75;\n        \n            vec2 p = layerUV * (2.0 * (0.8 + u_bass * 0.5));\n            vec2 v = p;\n            float l = 0.0;\n            vec3 c = vec3(0.0);\n            l = abs((0.7 * (0.8 + u_bass * 0.5)) - dot(p,p));\n            v = p * (1.0 - l) / (0.2 * u_distortion);\n            for(float i=0.0; i<(8.0 * (0.8 + u_bass * 0.5)); i++) {\n                c += (sin(vec3(v.x, v.y, v.y) * 2.0) + 1.0) * abs(v.x - v.y) * (0.2 * u_intensity) + (u_bass * 1.5);\n                v += cos(v.yx * i + vec2(0.0, i) + localTime) / (i + 1.0) + (0.7 * u_distortion);\n            }\n            vec3 glow = exp(p.y * vec3(1.0, -1.0, -2.0)) * exp(-(4.0 * (0.8 + u_bass * 0.5)) * l);\n            // SAFE: max(c, 0.1) correct order\n            vec3 layerColor = tanh(glow / max(c, 0.1)) * (1.0 + u_bass);\n            \n        layerColor *= u_amplitude * (0.8 + u_bass * 0.4);\n        finalColor = layerColor;\n    }\n\n    // --- Layer 2: Tunnel Effect ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.5) * 0.1 * (1.0 + u_bass);\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.30;\n        \n            float r = 1.0/length(layerUV) + localTime;\n            float a = atan(layerUV.y, layerUV.x);\n            float v = sin(r * (10.0 * u_scale) + u_treble) * cos(a * 8.0);\n            vec3 layerColor = palette(v * 0.5 + (0.5 * (0.8 + u_treble * 0.5)));\n             \n        layerColor *= u_amplitude * (0.8 + u_treble * 0.4);\n        finalColor = blend_Exclusion(finalColor, layerColor, 0.5 + u_treble * 0.2);\n    }\n\n    // --- Layer 3: Fluid Dynamics ---\n    {\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.74;\n        \n            vec2 p = layerUV * 2.0;\n            for(int i=1; i<4; i++) {\n                float t = localTime * float(i) * 0.2;\n                p += vec2(0.7/float(i)*sin(float(i)*p.y + t + 0.3*float(i)) + (0.8 * (0.8 + u_bass * 0.5)), \n                          (0.4 * u_intensity)/float(i)*sin(float(i)*p.x + t + 0.3*float(i) + 1.6));\n            }\n            vec3 layerColor = vec3((0.5 * (0.8 + u_bass * 0.5))*sin(p.x)+0.5, 0.5*sin(p.y)+0.5, sin(p.x+p.y));\n            \n        layerColor *= u_amplitude * (0.8 + u_bass * 0.4);\n        finalColor = blend_SoftLight(finalColor, layerColor, 0.5 + u_bass * 0.2);\n    }\n\n    // --- Layer 4: Space Distortion ---\n    {\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.95;\n        \n            float dist = length(layerUV);\n            float angle = atan(layerUV.y, layerUV.x);\n            angle += sin(dist * 10.0 - localTime) * (0.5 * (0.8 + u_mid * 0.5)) * u_mid;\n            layerUV = vec2(cos(angle), sin(angle)) * dist;\n            vec3 layerColor = vec3(0.0); \n            \n        layerColor *= u_amplitude * (0.8 + u_mid * 0.4);\n        finalColor = blend_Exclusion(finalColor, layerColor, 0.5 + u_mid * 0.2);\n    }\n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 + u_time) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = 0.95; \n    #ifdef u_decay\n        dcAmount = u_decay;\n    #endif\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = 0.0;\n    #ifdef u_feedback\n        fbAmount = u_feedback;\n    #endif\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.7 || u_beat > 0.9) {\n        // Force feedback ON during the beat\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.778Z"
    },
    {
      "index": 7,
      "name": "Chikaka",
      "thumbnail": "data:image/jpeg;base64,",
      "params": {
        "speed": 0,
        "intensity": 1.5,
        "distortion": 0.2,
        "colorShift": 1.7,
        "amplitude": 1
      },
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform float u_beat;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\nuniform float u_speed;\nuniform float u_intensity;\nuniform float u_distortion;\nuniform float u_colorShift;\nuniform float u_amplitude;\n\n// Defaults for missing sliders\n#define u_glow 1.0\n#define u_complexity 1.0\n#define u_rotation 0.0\n#define u_scale 1.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n#define u_feedback 0.0\n#define u_decay 0.95\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            return texture(u_spectrum, vec2(clamp(f, 0.01, 0.99), 0.0)).r;\n        }\n        \n\n                vec3 palette(float t) {\n                    return 0.5 + 0.5 * cos(6.28318 * (t + vec3(0.0, 0.33, 0.67)));\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, 4);\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    \n                uv += (vec2(\n                    noise(uv * 2.0 + u_time * 0.2),\n                    noise(uv * 2.0 - u_time * 0.2)\n                ) - 0.5) * u_distortion * 0.5;\n            \n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    // --- Layer 1: Wave Patterns ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.47;\n        \n             float scan = sin(layerUV.y * (100.0 * (0.8 + u_beat * 0.5)) + localTime * 10.0);\n             float wave = sin(layerUV.x * 5.0 + localTime) * 0.2;\n             float beam = smoothstep((0.01 * u_distortion), 0.0, abs(layerUV.y - wave));\n             vec3 layerColor = vec3((0.2 * u_intensity), 1.0, 0.2) * (beam + scan * 0.1);\n             \n        layerColor *= u_amplitude * (0.8 + u_beat * 0.4);\n        finalColor = layerColor;\n    }\n\n    // --- Layer 2: Geometric Shapes ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.5) * 0.1 * (1.0 + u_bass);\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.79;\n        \n            vec2 p = abs(layerUV) - 0.5;\n            float d = length(p);\n            float s = sin(d * (20.0 * (0.8 + u_treble * 0.5)) - localTime * (4.0 * u_amplitude) + u_beat);\n            s = smoothstep(0.4, 0.5, s);\n            vec3 layerColor = palette(d) * s;\n            \n        layerColor *= u_amplitude * (0.8 + u_treble * 0.4);\n        finalColor = blend_Difference(finalColor, layerColor, 0.5 + u_treble * 0.2);\n    }\n\n    // --- Layer 3: Fractal Patterns ---\n    {\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.51;\n        \n            vec2 p = layerUV * (1.5 * (0.8 + u_bass * 0.5));\n            vec2 c = vec2(sin(localTime*(0.3 * u_colorShift)), cos(localTime*0.4));\n            float i_val = 0.0;\n            for(int i=0; i<6; i++) {\n                p = vec2(p.x*p.x - p.y*p.y, 2.0*p.x*p.y) + c;\n                if(length(p) > 4.0) break;\n                i_val += 1.0;\n            }\n            vec3 layerColor = palette(i_val * (0.1 * (0.8 + u_bass * 0.5)) + localTime);\n            \n        layerColor *= u_amplitude * (0.8 + u_bass * 0.4);\n        finalColor = blend_Exclusion(finalColor, layerColor, 0.5 + u_bass * 0.2);\n    }\n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 + u_time) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = 0.95; \n    #ifdef u_decay\n        dcAmount = u_decay;\n    #endif\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = 0.0;\n    #ifdef u_feedback\n        fbAmount = u_feedback;\n    #endif\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.7 || u_beat > 0.9) {\n        // Force feedback ON during the beat\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.778Z"
    },
    {
      "index": 8,
      "name": "Sag A*",
      "thumbnail": "data:image/jpeg;base64,",
      "params": {
        "speed": 0.7,
        "intensity": 1.1,
        "scale": 1.01,
        "distortion": 0.4,
        "glow": 1.2
      },
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform float u_beat;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\nuniform float u_speed;\nuniform float u_intensity;\nuniform float u_scale;\nuniform float u_distortion;\nuniform float u_glow;\n\n// Defaults for missing sliders\n#define u_amplitude 1.0\n#define u_complexity 1.0\n#define u_colorShift 0.0\n#define u_rotation 0.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n#define u_feedback 0.0\n#define u_decay 0.95\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            return texture(u_spectrum, vec2(clamp(f, 0.01, 0.99), 0.0)).r;\n        }\n        \n\n                vec3 palette(float t) {\n                    return vec3(0.9, 0.8, 0.8) * (0.5 + 0.5 * cos(6.28318 * t + vec3(0.0, 0.1, 0.2)));\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, 4);\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    uv /= (u_scale * 0.5 + 0.1 + u_bass * 0.05);\n    \n                uv += (vec2(\n                    noise(uv * 2.0 + u_time * 0.2),\n                    noise(uv * 2.0 - u_time * 0.2)\n                ) - 0.5) * u_distortion * 0.5;\n            \n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    // --- Layer 1: Spectrum Analyzer ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.67;\n        \n            float fIndex = abs(layerUV.x); \n            float val = getFreq(fIndex);\n            float barWidth = 0.05;\n            float barID = floor(fIndex / barWidth);\n            float qVal = getFreq(barID * barWidth + barWidth * 0.5);\n            float bar = step(abs(layerUV.y), qVal * 0.8);\n            float grid = step(0.002, abs(mod(layerUV.x, barWidth) - barWidth * 0.5));\n            bar *= grid;\n            vec3 layerColor = palette(barID * 0.1 + localTime) * bar;\n            layerColor += vec3((0.2 * u_distortion), 0.5, 1.0) * bar * u_bass;\n            \n        layerColor *= u_amplitude * (0.8 + u_bass * 0.4);\n        finalColor = layerColor;\n    }\n\n    // --- Layer 2: Bio-Digital (Xor) ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.01) ;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.0;\n        \n            vec3 rayDir = normalize(vec3(layerUV, -1.0));\n            vec3 p = vec3(0.0);\n            vec3 v = vec3(0.0);\n            vec3 col = vec3(0.0);\n            float z = 0.0;\n            float d = 0.0;\n            float t = localTime * u_bass * .01;\n\n            for(float i=0.0; i<50.0; i++) {\n                p = z * rayDir;\n                p.xz -= t;\n                v = p - vec3(sin(p.x), sin(p.x), sin(p.z));\n                float dotProd = dot(cos(v.xz), sin(v.zx / 0.6));\n                d = 0.4 * max(dotProd + 0.6, v.y + 3.0);\n                \n                vec3 fog = -rayDir * d * d / (z * z + 1.0);\n                vec3 pal = cos(p.y + vec3(6.0, 1.0, 2.0)) + 1.1;\n                vec2 trig = tan(p.y / 0.3) / (cos(p.xz / 0.1) + 0.1 + (0.01 * u_mid));\n                float lightStruct = length(trig) + d * d / 0.01;\n                \n                // SAFE: Ensure denominator is never zero\n                vec3 light = pal / (lightStruct + 0.01) / (z + 0.1 / (u_mid + 0.1));\n\n                col += (fog * u_mid) + (light * u_mid);\n                z += max(0.02, d); \n            }\n            vec3 layerColor = tanh(col * 0.1);\n            \n        layerColor *= u_amplitude * (0.8 + u_mid * 0.4);\n        finalColor = blend_SoftLight(finalColor, layerColor, 0.2 + u_mid * 0.9);\n    }\n\n    // --- Layer 3: Space Distortion ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.01) * 0.01 * (.10 + u_bass);\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.74;\n        \n            layerUV = abs(layerUV);\n            layerUV -= 0.25;\n            layerUV = rotate(layerUV, localTime * (0.1 * u_glow));\n            vec3 layerColor = vec3(0.0);\n            \n        layerColor *= u_amplitude * (0.8 + u_treble * 0.4);\n        finalColor = blend_SoftLight(finalColor, layerColor, 0.5 + u_treble * 0.9);\n    }\n\n    // --- Layer 4: Tunnel Effect ---\n    {\n                currentUV += (vec2(finalColor.r, finalColor.g) - 0.01);\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.47;\n        \n            vec3 rayDir = normalize(vec3(layerUV, -1.0));\n            vec3 p = vec3(0.0);\n            vec4 col = vec4(0.0);\n            float z = 0.0;\n            float d = 0.0;\n            float t = localTime * .10;\n            for(float i=0.0; i<20.0; i++) {\n                p = z * rayDir;\n                float angle = atan(p.y / 0.2, p.x) * 2.0;\n                float depth = p.z / 3.0;\n                float radius = length(p.xy) - 5.0 - z * 0.2;\n                p = vec3(angle, depth, radius);\n                for(float j=1.0; j<7.0; j++) {\n                    p += sin(p.yzx * j + t + 0.3 * i) / j;\n                }\n                vec4 distVec = vec4(0.4 * cos(p) - (0.4 * (0.8 + u_mid * 0.5)), p.z);\n                d = length(distVec);\n                z += d;\n                vec4 pal = cos(p.x + i * (0.4 * (0.8 + u_mid * 0.5)) + z + vec4(6.0, 1.0, (2.0 * u_glow), 0.0)) + (1.0 + u_mid);\n                col += pal / max(0.001, (d + u_mid * 0.5)); \n            }\n            vec3 layerColor = tanh(col.rgb * col.rgb / 400.0);\n            \n        layerColor *= u_amplitude * (0.8 + u_mid * 0.4);\n        finalColor = blend_Screen(finalColor, layerColor, 0.5 + u_mid * 0.6);\n    }\n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 + u_time) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = 0.95; \n    #ifdef u_decay\n        dcAmount = u_decay;\n    #endif\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = 0.0;\n    #ifdef u_feedback\n        fbAmount = u_feedback;\n    #endif\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.7 || u_beat > 0.9) {\n        // Force feedback ON during the beat\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.778Z"
    },
    {
      "index": 9,
      "name": "Frida",
      "thumbnail": "data:image/jpeg;base64,",
      "params": {
        "speed": 1
      },
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform float u_beat;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\nuniform float u_speed;\n\n// Defaults for missing sliders\n#define u_intensity 1.0\n#define u_amplitude 1.0\n#define u_glow 1.0\n#define u_complexity 1.0\n#define u_colorShift 0.0\n#define u_distortion 0.0\n#define u_rotation 0.0\n#define u_scale 1.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n#define u_feedback 0.0\n#define u_decay 0.95\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            return texture(u_spectrum, vec2(clamp(f, 0.01, 0.99), 0.0)).r;\n        }\n        \n\n                vec3 palette(float t) {\n                    return 0.5 + 0.5 * cos(6.28318 * (t + vec3(0.0, 0.33, 0.67)));\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, 4);\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    \n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    // --- Layer 1: Fractal Patterns ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.0;\n        \n            vec2 p = layerUV * 2.0;\n            float a = 0.0;\n            for(int i=0; i<5; i++) {\n                p = abs(p) / dot(p,p) - 0.5;\n                p = rotate(p + 0.01 + (u_time * 0.00005), localTime * 0.1);\n                a += length(p);\n            }\n            vec3 layerColor = palette(a * 0.2 + u_mid) * ((.5 * u_bass) + 0.5 * sin(a));\n            \n        layerColor *= u_amplitude * (0.8 + u_treble * 0.4);\n        finalColor = layerColor;\n    }\n\n    // --- Layer 2: Tunnel Effect ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.5) * 0.1 * (1.0 + u_bass);\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.59;\n        \n            vec3 rayDir = normalize(vec3(layerUV, -1.0));\n            vec3 p = vec3(0.0);\n            vec3 col = vec3(0.0);\n            float z = 0.0;\n            float t = localTime * 2.0;\n            for(int i=0; i<20; i++) {\n                p = z * rayDir;\n                vec3 a = p;\n                for(float j=2.0; j<7.0; j++) {\n                    a -= sin(a * j + t + float(i)).yzx / j;\n                }\n                vec3 ap = abs(p);\n                float d_box = abs(2.0 - max(ap.x, ap.y)); \n                float s = a.z + a.y - t;\n                float d_detail = abs(cos(s)) / 7.0;\n                float d = d_box + d_detail;\n                vec3 pal = cos(vec3(s - z) + vec3(0.0, 1.0, 8.0)) + 1.0;\n                col += pal / max(0.001, d); \n                z += max(0.05, d);\n            }\n            vec3 layerColor = tanh(col * 0.005);\n            \n        layerColor *= u_amplitude * (0.8 + u_beat * 0.4);\n        finalColor = blend_Screen(finalColor, layerColor, 0.5 + u_beat * 0.2);\n    }\n\n    // --- Layer 3: Particle System ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.01) * 0.1 * (0.1 + u_bass);\n        \n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.17;\n        \n            vec3 col = vec3(0.0);\n            for(float i=0.0; i<8.0; i++) {\n                 float t = localTime + i * 0.5;\n                 vec2 p = vec2(cos(t), sin(t)) * (0.2 + i * 0.05);\n                 float d = length(layerUV - p);\n                 col += palette(i*0.1) * (0.01 / d);\n            }\n            vec3 layerColor = col;\n            \n        layerColor *= u_amplitude * (0.8 + u_bass * 0.4);\n        finalColor = blend_Difference(finalColor, layerColor, 0.5 + u_bass * 0.2);\n    }\n\n    // --- Layer 4: Fluid Dynamics ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.01) * 0.1 * (0.1 + u_bass);\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.78;\n        \n            vec2 p = layerUV * 2.0;\n            for(int i=1; i<4; i++) {\n                float t = localTime * float(i) * 0.2;\n                p += vec2(0.7/float(i)*sin(float(i)*p.y + t + 0.3*float(i)) + 0.8, \n                          0.4/float(i)*sin(float(i)*p.x + t + 0.3*float(i) + 1.6));\n            }\n            vec3 layerColor = vec3(0.5*sin(p.x)+0.5, 0.5*sin(p.y)+0.5, sin(p.x+p.y));\n            \n        layerColor *= u_amplitude * (0.8 + u_mid * 0.4);\n        finalColor = blend_SoftLight(finalColor, layerColor, 0.5 + u_mid * 0.2);\n    }\n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 + u_time) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = 0.95; \n    #ifdef u_decay\n        dcAmount = u_decay;\n    #endif\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = 0.0;\n    #ifdef u_feedback\n        fbAmount = u_feedback;\n    #endif\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.7 || u_beat > 0.9) {\n        // Force feedback ON during the beat\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.778Z"
    },
    {
      "index": 10,
      "name": "Speckers",
      "thumbnail": "data:image/jpeg;base64,",
      "params": {
        "speed": 0.8,
        "intensity": 3.7,
        "scale": 0.71,
        "complexity": 0.7
      },
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\nuniform float u_speed;\nuniform float u_intensity;\nuniform float u_scale;\nuniform float u_complexity;\n\n// Defaults for missing sliders\n#define u_amplitude 1.0\n#define u_glow 1.0\n#define u_colorShift 0.0\n#define u_distortion 0.0\n#define u_rotation 0.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n#define u_feedback 0.0\n#define u_decay 0.95\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            float freq = clamp(f, 0.01, 0.99);\n            float freqValue = texture(u_spectrum, vec2(freq, 0.0)).r;\n            \n            // Fallback: create demo pattern when no audio (spectrum is all zeros)\n            // This creates a gentle wave pattern so effects are visible in preview\n            float demo = 0.3 + 0.2 * sin(freq * 10.0 + u_time * 2.0) \n                             + 0.15 * sin(freq * 25.0 + u_time * 3.0);\n            \n            // Mix between real audio and demo based on audio level\n            float audioLevel = freqValue;\n            return mix(demo, freqValue, step(0.01, audioLevel));\n        }\n        \n\n                vec3 palette(float t) {\n                    return 0.5 + 0.5 * cos(6.28318 * (t + vec3(0.0, 0.33, 0.67)));\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, int(u_complexity * 4.0 + 2.0));\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    uv /= (u_scale * 0.5 + 0.1 + u_bass * 0.05);\n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    // --- Layer 1: Spectrum Analyzer ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.18;\n        \n            vec2 p = layerUV;\n            \n            // Create scrolling effect\n            float scrollSpeed = (0.5 * (0.1 + u_treble * 0.1));\n            float scrollY = fract(p.y + localTime * scrollSpeed);\n            \n            // Get frequency based on X position\n            float fIndex = abs(p.x);\n            float val = getFreq(fIndex);\n            \n            // Current bar (most recent, at top)\n            float barIntensity = smoothstep(0.02, 0.0, abs(scrollY - (0.95 * (0.8 + u_treble * 0.5)))) * val;\n            \n            // Add fading trail effect\n            float trail = 0.0;\n            for(float i = 1.0; i < 8.0; i++) {\n                float trailY = fract(scrollY - i * (0.12 * (0.8 + u_treble * 0.5)));\n                float trailFreq = getFreq(fIndex + i * (0.001 * (0.1 + u_treble * 0.1))); // Slight frequency shift for variation\n                trail += smoothstep((0.5 * u_intensity), 0.0, abs(trailY - 0.95)) * trailFreq * (1.0 - i * 0.1);\n            }\n            \n            // Color based on frequency position (bass = left, treble = right)\n            vec3 layerColor = palette(fIndex + p.y * 0.5) * barIntensity;\n            layerColor += palette(fIndex * 0.5 + 0.5) * trail * 0.6;\n            \n            // Add bass glow\n            layerColor += vec3(0.8, 0.2, 0.4) * barIntensity * u_treble * 0.3;\n            \n        layerColor *= u_amplitude * (0.5 + u_treble * 0.4);\n        finalColor = layerColor;\n    }\n\n    // --- Layer 2: Fractal Patterns ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.01) * 0.1 * (0.1 + u_bass);\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.81;\n        \n            vec2 p = layerUV;\n            p = p / (u_scale + (0.1 * (0.8 + u_mid * 0.5))) - vec2(0.7, 0.0); \n            vec2 c = p;\n            vec2 z = vec2(0.0);\n            float iter = 0.0;\n            for(int i=0; i < int(float(8) * u_complexity); i++) {\n                z = vec2(z.x*z.x - z.y*z.y, 2.0*z.x*z.y) + c;\n                if(length(z) > 2.0) break;\n                iter += 1.0;\n            }\n            vec3 layerColor = palette(iter/8.0 + localTime * 0.2) * u_mid;\n            \n        layerColor *= u_amplitude * (0.8 + u_mid * 0.4);\n        finalColor = blend_Overlay(finalColor, layerColor, 0.5 + u_mid * 0.2);\n    }\n\n    // --- Layer 3: Fluid Dynamics ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.01) * 0.1 * (0.1 + u_bass);\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.83;\n        \n            float d = length(layerUV);\n            float u = noise(layerUV * 5.0 + localTime);\n            float v = noise(layerUV * 10.0 - localTime);\n            float f = 0.5 + 0.5 * sin(10.0 * (u - v));\n            vec3 layerColor = mix(vec3((0.1 * u_complexity), 0.0, 0.2), vec3(0.0, 1.0, 0.8), f) * (1.0-d);\n            \n        layerColor *= u_amplitude * (0.8 + u_treble * 0.4);\n        finalColor = blend_Overlay(finalColor, layerColor, 0.5 + u_treble * 0.2);\n    }\n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 + u_time) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = 0.95; \n    #ifdef u_decay\n        dcAmount = u_decay;\n    #endif\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = 0.0;\n    #ifdef u_feedback\n        fbAmount = u_feedback;\n    #endif\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.7) {\n        // Force feedback ON during high energy\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.778Z"
    },
    {
      "index": 11,
      "name": "CyberVoid",
      "thumbnail": "data:image/jpeg;base64,",
      "params": {},
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\n\n\n// Defaults for missing sliders\n#define u_speed 1.0\n#define u_intensity 1.0\n#define u_amplitude 1.0\n#define u_glow 1.0\n#define u_complexity 1.0\n#define u_colorShift 0.0\n#define u_distortion 0.0\n#define u_rotation 0.0\n#define u_scale 1.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n#define u_feedback 0.0\n#define u_decay 0.95\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            float freq = clamp(f, 0.01, 0.99);\n            float freqValue = texture(u_spectrum, vec2(freq, 0.0)).r;\n            \n            // Fallback: create demo pattern when no audio (spectrum is all zeros)\n            // This creates a gentle wave pattern so effects are visible in preview\n            float demo = 0.3 + 0.2 * sin(freq * 10.0 + u_time * 2.0) \n                             + 0.15 * sin(freq * 25.0 + u_time * 3.0);\n            \n            // Mix between real audio and demo based on audio level\n            float audioLevel = freqValue;\n            return mix(demo, freqValue, step(0.01, audioLevel));\n        }\n        \n\n                vec3 palette(float t) {\n                    return 0.5 + 0.5 * cos(6.28318 * (t + vec3(0.0, 0.33, 0.67)));\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, 4);\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    \n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    // --- Layer 1: Bio-Digital (Xor) [Xor Neural] ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * 0.56;\n        \n            // Original: vec3 p,v;for(float i,z,d,l;i++<1e2;o+=(sin(p.y+vec4(6,1,3,3))+1.)/d)p=z*normalize(FC.rgb*2.-r.xyy),p.z+=9.,p=vec3(atan(p.z,p.x)-t*.1,log(l=length(p))-t*.2,asin(p.y/l))/.1,z+=d=l/6e1*length(max(v=cos(p+sin(p/.24+t)),v.yzx*.1));o=tanh(o/2e4);\n            vec3 o = vec3(0.0);\n            vec3 p = vec3(0.0);\n            vec3 v = vec3(0.0);\n            \n            float z_pos = 0.0;\n            float d = 0.0;\n            float l = 0.0;\n            float t = localTime * 0.5;\n            \n            // Create 3D fragment coordinates (Shadertoy-style FC.rgb)\n            vec3 FC = vec3(gl_FragCoord.xy, 0.0);\n            vec3 r = vec3(u_resolution.xy, u_resolution.x);\n            \n            // Raymarching loop (100 iterations)\n            for(float i = 0.0; i < 100.0; i++) {\n                // p=z*normalize(FC.rgb*2.-r.xyy)\n                p = z_pos * normalize(FC * 2.0 - r.xyy);\n                \n                // p.z+=9.\n                p.z += 9.0;\n                \n                // p=vec3(atan(p.z,p.x)-t*.1,log(l=length(p))-t*.2,asin(p.y/l))/.1\n                l = length(p);\n                p = vec3(atan(p.z, p.x) - t * 0.2, log(l) - t * 0.2, asin(clamp(p.y / l, -1.0, 1.0))) / 0.1;\n                \n                // z+=d=l/6e1*length(max(v=cos(p+sin(p/.24+t)),v.yzx*.1))\n                v = cos(p + sin(p / 0.24 + t));\n                vec3 v_yzx_select = v.yzx * 0.1 + (u_treble  * 0.01);\n                d = l / 60.0 * length(max(v, v_yzx_select));\n                z_pos += d;\n                \n                // o+=(sin(p.y+vec4(6,1,3,3))+1.)/d\n                // Use .xyz to convert vec4 result to vec3\n                o += (sin(vec4(p.y) + vec4(6.0, 1.0, 3.0, 3.0)) + 0.10 + u_bass ).xyz / d;\n            }\n            \n            // o=tanh(o/2e4)\n            vec3 layerColor = tanh(o / 20000.0);\n            \n            // Audio reactivity\n            float intens = 1.0;\n            #ifdef u_intensity\n                intens = u_intensity;\n            #endif\n            layerColor *= intens * (1.0 + u_mid * 0.5 + u_mid * 0.3);\n            \n        layerColor *= u_amplitude * (0.8 + u_mid * 0.4);\n        finalColor = layerColor;\n    }\n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 + u_time) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = 0.95; \n    #ifdef u_decay\n        dcAmount = u_decay;\n    #endif\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = 0.0;\n    #ifdef u_feedback\n        fbAmount = u_feedback;\n    #endif\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.7) {\n        // Force feedback ON during high energy\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.778Z"
    },
    {
      "index": 12,
      "name": "Reef",
      "thumbnail": "data:image/jpeg;base64,",
      "params": {},
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\n\n\n// Defaults for missing sliders\n#define u_speed 1.0\n#define u_intensity 1.0\n#define u_amplitude 1.0\n#define u_glow 1.0\n#define u_complexity 1.0\n#define u_colorShift 0.0\n#define u_distortion 0.0\n#define u_rotation 0.0\n#define u_scale 1.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n#define u_feedback 0.0\n#define u_decay 0.95\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            float freq = clamp(f, 0.01, 0.99);\n            float freqValue = texture(u_spectrum, vec2(freq, 0.0)).r;\n            \n            // Fallback: create demo pattern when no audio (spectrum is all zeros)\n            // This creates a gentle wave pattern so effects are visible in preview\n            float demo = 0.3 + 0.2 * sin(freq * 10.0 + u_time * 2.0) \n                             + 0.15 * sin(freq * 25.0 + u_time * 3.0);\n            \n            // Mix between real audio and demo based on audio level\n            float audioLevel = freqValue;\n            return mix(demo, freqValue, step(0.01, audioLevel));\n        }\n        \n\n                vec3 palette(float t) {\n                    return 0.5 + 0.5 * cos(6.28318 * (t + vec3(0.0, 0.33, 0.67)));\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, 4);\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    \n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    // --- Layer 1: Bio-Digital (Xor) [Reef] ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * 1.09;\n        \n            vec3 rayDir = normalize(vec3(layerUV, -1.0));\n            vec3 p = vec3(0.0);\n            vec3 col = vec3(0.0);\n            float z = 0.0;\n            float d = 0.0;\n            \n            // Audio reactive time \n            float t = localTime + u_mid * 1.5;\n\n            for(float i = 1.0; i <= 40.0; i++) {\n                p = z * rayDir;\n                \n                // Inner volumetric distortion loop\n                for(float j = 1.0; j <= 9.0; j++) {\n                    p += 0.4 * sin(p.yzx * j - z + t + i) / j + 0.5;\n                }\n                \n                // Shape bounds function\n                vec4 distVec = vec4(abs(p.y + p.z * 0.5), sin(p - z) / 7.0);\n                d = length(distVec) / (4.0 + z * z / 100.0);\n                \n                // Original code effectively evaluates z += d *before* color division\n                z += d;\n                \n                vec3 pal = 0.9 + sin(i * 0.1 - vec3(6.0, 1.0, 2.0));\n                \n                // SAFE: Ensure denominator is never pure zero\n                float denom = max(0.001, d * d * z);\n                \n                vec3 glow1 = pal / denom;\n                vec3 glow2 = (d * z) / vec3(4.0, 2.0, 1.0); \n                \n                col += glow1 + glow2;\n            }\n            \n            float intens = 1.0;\n            #ifdef u_intensity\n                intens = u_intensity;\n            #endif\n            \n            // Scaled by 2000 as per original (o / 2e3), padded with gentle audio dynamics\n            vec3 layerColor = tanh((col / 2000.0) * intens * (1.0 + u_mid * 0.2));\n            \n        layerColor *= u_amplitude * (0.8 + u_mid * 0.4);\n        finalColor = layerColor;\n    }\n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 + u_time) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = 0.95; \n    #ifdef u_decay\n        dcAmount = u_decay;\n    #endif\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = 0.0;\n    #ifdef u_feedback\n        fbAmount = u_feedback;\n    #endif\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.7) {\n        // Force feedback ON during high energy\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.778Z"
    },
    {
      "index": 13,
      "name": "Partickles",
      "thumbnail": "data:image/jpeg;base64,",
      "params": {
        "speed": -0.1,
        "intensity": 1.1,
        "scale": 0.71,
        "colorShift": 1.4,
        "glow": 0
      },
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\nuniform float u_speed;\nuniform float u_intensity;\nuniform float u_scale;\nuniform float u_colorShift;\nuniform float u_glow;\n\n// Defaults for missing sliders\n#define u_amplitude 1.0\n#define u_complexity 1.0\n#define u_distortion 0.0\n#define u_rotation 0.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n#define u_feedback 0.0\n#define u_decay 0.95\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            float freq = clamp(f, 0.01, 0.99);\n            float freqValue = texture(u_spectrum, vec2(freq, 0.0)).r;\n            \n            // Fallback: create demo pattern when no audio (spectrum is all zeros)\n            // This creates a gentle wave pattern so effects are visible in preview\n            float demo = 0.3 + 0.2 * sin(freq * 10.0 + u_time * 2.0) \n                             + 0.15 * sin(freq * 25.0 + u_time * 3.0);\n            \n            // Mix between real audio and demo based on audio level\n            float audioLevel = freqValue;\n            return mix(demo, freqValue, step(0.01, audioLevel));\n        }\n        \n\n                vec3 palette(float t) {\n                    vec3 a = vec3(0.5, 0.5, 0.5);\n                    vec3 b = vec3(0.5, 0.5, 0.5);\n                    vec3 c = vec3(2.0, 1.0, 0.0);\n                    vec3 d = vec3(0.5, 0.2, 0.25);\n                    return a + b * cos(6.28318 * (c * t + d));\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, 4);\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    uv /= (u_scale * 0.5 + 0.1 + u_bass * 0.05);\n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    // --- Layer 1: Particle System [Particles] ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.0;\n        \n            vec3 rayDir = normalize(vec3(layerUV, -1.0));\n            vec3 p = vec3(0.0);\n            vec3 col = vec3(0.0);\n            float z = 0.0;\n            float d = 0.0;\n            \n            // Audio reactive time \n            float t = localTime * .10 ;\n\n            for(float i = 1.0; i <= (20.0 * u_scale); i++) {\n                // 'f' defines a structural bounding depth scalar\n                float f = abs(z - 5.0) * (0.05 * u_scale);\n                \n                // The original code packed a float and a vec3 into a vec4 just to get the length.\n                // Unpacking it here is cleaner, prevents alpha-channel issues, and performs better:\n                float a = dot(sin(p), sin(p / (0.6 * (0.5)))) - length(p) + 5.0;\n                a += u_bass * 0.5;\n                vec3 b = sin(p / (0.1 * (0.8 - u_mid * 0.03 )) + t) * cos(p.yzx / (0.1 * (0.1)) + t) * (0.3 * (0.1 + u_bass * 0.2));\n                \n                // Calculate equivalent distance safely\n                float dist = sqrt(a * a + dot(b, b));\n                \n                d = 0.5 * max(f, dist - f);\n                z += d ;\n                \n                // Color palette (d / (0.2 * u_intensity) is replaced with d * (5.0 * (0.8)) for multiplication speed)\n                vec3 pal = cos(p.y + i * 0.2 + u_bass + d * 5.0 + vec3(0.0, 1.0, 2.0)) + 1.618 * u_treble;\n                \n                // SAFE: Prevent divide-by-zero causing black pixels or NaN artifacts\n                col += pal / max(0.001, d);\n                \n                // Advance ray position for the NEXT iteration \n                // (This perfectly mimics Xor's third argument in the golfed for-loop)\n                p = z * rayDir;\n                p.z += 9.0 + u_treble; \n            }\n            \n            float intens = 1.0;\n            #ifdef u_intensity\n                intens = u_intensity;\n            #endif\n            \n            // Original used tanh(o*o/3e5)\n            // We scale it dynamically based on the u_intensity and u_treble audio\n            vec3 layerColor = tanh((col * col / 300000.0) * intens * (.50 + u_time * 0.002));\n            \n        layerColor *= u_amplitude * (0.7 + u_treble * 0.9);\n        finalColor = layerColor;\n    }\n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 ) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = 0.95; \n    #ifdef u_decay\n        dcAmount = u_decay;\n    #endif\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = 0.0;\n    #ifdef u_feedback\n        fbAmount = u_feedback;\n    #endif\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.7) {\n        // Force feedback ON during high energy\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.778Z"
    },
    {
      "index": 14,
      "name": "Mainframe",
      "thumbnail": "data:image/svg+xml;base64,",
      "params": {
        "speed": 0.6,
        "intensity": 1.9,
        "scale": 0.11,
        "complexity": 1.3
      },
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\nuniform float u_speed;\nuniform float u_intensity;\nuniform float u_scale;\nuniform float u_complexity;\n\n// Defaults for missing sliders\n#define u_amplitude 1.0\n#define u_glow 1.0\n#define u_colorShift 0.0\n#define u_distortion 0.0\n#define u_rotation 0.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n#define u_feedback 0.0\n#define u_decay 0.95\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            float freq = clamp(f, 0.01, 0.99);\n            float freqValue = texture(u_spectrum, vec2(freq, 0.0)).r;\n            \n            // Fallback: create demo pattern when no audio (spectrum is all zeros)\n            // This creates a gentle wave pattern so effects are visible in preview\n            float demo = 0.3 + 0.2 * sin(freq * 10.0 + u_time * 2.0) \n                             + 0.15 * sin(freq * 25.0 + u_time * 3.0);\n            \n            // Mix between real audio and demo based on audio level\n            float audioLevel = freqValue;\n            return mix(demo, freqValue, step(0.01, audioLevel));\n        }\n        \n\n                vec3 palette(float t) {\n                    return 0.5 + 0.5 * cos(6.28318 * (t + vec3(0.0, 0.33, 0.67)));\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, int(u_complexity * 4.0 + 2.0));\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    uv /= (u_scale * 0.5 + 0.1 + u_bass * 0.05);\n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    // --- Layer 1: Fractal Patterns [Mainframe] ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.74;\n        \n            // abs(layerUV) creates the 4-way kaleidoscopic mirroring effect\n            // Division by 0.4 in the original is equivalent to multiplying by 2.5\n            vec2 p = abs(layerUV) * 2.5;\n            vec3 col = vec3(0.0);\n            \n            // Audio reactive time \n            float t = localTime * 1.5 + u_bass * 1.0;\n\n            for(float i = 1.0; i <= (9.0 * (0.1 + u_bass * 0.5)); i++) {\n                vec2 v = p - i * 0.2;\n                \n                // Inner structural loop\n                for(float f = 1.0; f <= 7.0; f++) {\n                    // Calculate grid-like offsets using ceil()\n                    // v.yx swizzles the vector to swap X and Y components\n                    vec2 cell = ceil(v.yx + i * 0.1) * (9.0 * u_complexity) + t;\n                    vec2 offset = sin(cell) / f;\n                    \n                    // Add offset and swap X/Y again to create the weaving matrix effect\n                    v = (v + offset).yx;\n                }\n                \n                float l = length(sin(v));\n                \n                // Color palette (converted from golfed vec4 to standard vec3)\n                vec3 pal = cos(i * 0.3 + l - vec3(4.0 + u_bass, (5.0 * u_complexity), 6.0 + u_treble)) + 1.0;\n                \n                // SAFE: Prevent division by zero using max()\n                // Original used .02 / l / l which equals .02 / (l * l)\n                col += 0.02 * pal / max(0.0001, l * l);\n            }\n            \n            float intens = 1.0;\n            #ifdef u_intensity\n                intens = u_intensity;\n            #endif\n            \n            // Because we don't have backbuffer trails, we pad the color intensity slightly \n            // and use u_bass to make the data-streams pulse beautifully with the beat.\n            vec3 layerColor = max(tanh(col * intens * (.2 + u_bass * 0.8)), 0.0);\n            \n        layerColor *= u_amplitude * (0.8 + u_bass * 0.4);\n        finalColor = layerColor;\n    }\n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 + u_time) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = 0.95; \n    #ifdef u_decay\n        dcAmount = u_decay;\n    #endif\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = 0.0;\n    #ifdef u_feedback\n        fbAmount = u_feedback;\n    #endif\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.7) {\n        // Force feedback ON during high energy\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.778Z"
    },
    {
      "index": 15,
      "name": "Lattice",
      "thumbnail": "data:image/jpeg;base64,",
      "params": {
        "speed": -0.5,
        "intensity": 0.7,
        "scale": 0.41,
        "colorShift": 4.5
      },
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\nuniform float u_speed;\nuniform float u_intensity;\nuniform float u_scale;\nuniform float u_colorShift;\n\n// Defaults for missing sliders\n#define u_amplitude 1.0\n#define u_glow 1.0\n#define u_complexity 1.0\n#define u_distortion 0.0\n#define u_rotation 0.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n#define u_feedback 0.0\n#define u_decay 0.95\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            float freq = clamp(f, 0.01, 0.99);\n            float freqValue = texture(u_spectrum, vec2(freq, 0.0)).r;\n            \n            // Fallback: create demo pattern when no audio (spectrum is all zeros)\n            // This creates a gentle wave pattern so effects are visible in preview\n            float demo = 0.3 + 0.2 * sin(freq * 10.0 + u_time * 2.0) \n                             + 0.15 * sin(freq * 25.0 + u_time * 3.0);\n            \n            // Mix between real audio and demo based on audio level\n            float audioLevel = freqValue;\n            return mix(demo, freqValue, step(0.01, audioLevel));\n        }\n        \n\n                vec3 palette(float t) {\n                    return 0.5 + 0.5 * cos(6.28318 * (t + vec3(0.0, 0.33, 0.67)));\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, 4);\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    uv /= (u_scale * 0.5 + 0.1 + u_bass * 0.05);\n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    // --- Layer 1: Fluid Dynamics [Reef] ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.24;\n        \n            vec3 rayDir = normalize(vec3(layerUV, -1.0));\n            vec3 p = vec3(0.0);\n            vec3 col = vec3(0.0);\n            float z = 0.0;\n            float d = 0.0;\n            \n            // Audio reactive time \n            float t = localTime + u_mid * 1.5;\n\n            for(float i = 1.0; i <= (30.0 * u_intensity); i++) {\n                p = z * rayDir;\n                \n                // Inner volumetric distortion loop\n                for(float j = 1.0; j <= 3.0; j++) {\n                    p += (0.4 * u_scale) * sin(p.yzx * j - z + t + i) / j + (0.5 * (0.8 + u_mid * 0.5));\n                }\n                \n                // Shape bounds function\n                vec4 distVec = vec4(abs(p.y + p.z * 0.5), sin(p - z) / (7.0 * (0.8 + u_mid * 0.5)));\n                d = length(distVec) / (4.0 + z * z / (100.0 * u_scale));\n                \n                // Original code effectively evaluates z += d *before* color division\n                z += d;\n                \n                vec3 pal = (0.9 * (0.8 + u_mid * 0.5)) + sin(i * 0.1 - vec3((6.0 * (0.8 + u_mid * 0.5)), 1.0, 2.0));\n                \n                // SAFE: Ensure denominator is never pure zero\n                float denom = max(0.001, d * d * z);\n                \n                vec3 glow1 = pal / denom;\n                vec3 glow2 = (d * z) / vec3((4.0 * u_scale), 2.0, 1.0); \n                \n                col += glow1 + glow2;\n            }\n            \n            float intens = 1.0;\n            #ifdef u_intensity\n                intens = u_intensity;\n            #endif\n            \n            // Scaled by 2000 as per original (o / 2e3), padded with gentle audio dynamics\n            vec3 layerColor = tanh((col / 2000.0) * intens * (1.0 + u_mid * (0.2 * u_colorShift)));\n            \n        layerColor *= u_amplitude * (0.8 + u_mid * 0.4);\n        finalColor = layerColor;\n    }\n\n    // --- Layer 2: Fractal Patterns [Mainframe] ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.71;\n        \n            // abs(layerUV) creates the 4-way kaleidoscopic mirroring effect\n            // Division by 0.4 in the original is equivalent to multiplying by (2.5 * u_colorShift)\n            vec2 p = abs(layerUV) * 2.5;\n            vec3 col = vec3(0.0);\n            \n            // Audio reactive time \n            float t = localTime * 1.0;\n\n            for(float i = 1.0; i <= 9.0; i++) {\n                vec2 v = p - i * 0.2;\n                \n                // Inner structural loop\n                for(float f = 1.0; f <= 7.0; f++) {\n                    // Calculate grid-like offsets using ceil()\n                    // v.yx swizzles the vector to swap X and Y components\n                    vec2 cell = ceil(v.yx + i * 0.1) * 9.0 + t;\n                    vec2 offset = sin(cell) / f;\n                    \n                    // Add offset and swap X/Y again to create the weaving matrix effect\n                    v = (v + offset).yx;\n                }\n                \n                float l = length(sin(v));\n                \n                // Color palette (converted from golfed vec4 to standard vec3)\n                vec3 pal = cos(i * 0.3 + u_bass * 1.5 + 0.5 - vec3(4.0, (5.0 * (0.8 + u_mid * 0.5)), 6.0)) + 1.0;\n                \n                // SAFE: Prevent division by zero using max()\n                // Original used .02 / l / l which equals .02 / (l * l)\n                col += (0.02 * u_scale) * pal / max(0.0001, l * l);\n            }\n            \n            float intens = 1.0;\n            #ifdef u_intensity\n                intens = u_intensity;\n            #endif\n            \n            // Because we don't have backbuffer trails, we pad the color intensity slightly \n            // and use u_mid to make the data-streams pulse beautifully with the beat.\n            vec3 layerColor = max(tanh(col * intens * (1.2 + u_mid * 0.8)), 0.0);\n            \n        layerColor *= u_amplitude * (0.8 + u_mid * 0.4);\n        finalColor = blend_Add(finalColor, layerColor, 0.5 + u_mid * 0.2);\n    }\n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 + u_time) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = 0.95; \n    #ifdef u_decay\n        dcAmount = u_decay;\n    #endif\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = 0.0;\n    #ifdef u_feedback\n        fbAmount = u_feedback;\n    #endif\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.3) {\n        // Force feedback ON during high energy\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.778Z"
    },
    {
      "index": 16,
      "name": "Milky",
      "thumbnail": "data:image/jpeg;base64,",
      "params": {
        "intensity": 0.3,
        "scale": 0.81,
        "complexity": 1.2
      },
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\nuniform float u_intensity;\nuniform float u_scale;\nuniform float u_complexity;\n\n// Defaults for missing sliders\n#define u_speed 1.0\n#define u_amplitude 1.0\n#define u_glow 1.0\n#define u_colorShift 0.0\n#define u_distortion 0.0\n#define u_rotation 0.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n#define u_feedback 0.0\n#define u_decay 0.95\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            float freq = clamp(f, 0.01, 0.99);\n            float freqValue = texture(u_spectrum, vec2(freq, 0.0)).r;\n            \n            // Fallback: create demo pattern when no audio (spectrum is all zeros)\n            // This creates a gentle wave pattern so effects are visible in preview\n            float demo = 0.3 + 0.2 * sin(freq * 10.0 + u_time * 2.0) \n                             + 0.15 * sin(freq * 25.0 + u_time * 3.0);\n            \n            // Mix between real audio and demo based on audio level\n            float audioLevel = freqValue;\n            return mix(demo, freqValue, step(0.01, audioLevel));\n        }\n        \n\n                vec3 palette(float t) {\n                    vec3 a = vec3(0.5, 0.5, 0.5);\n                    vec3 b = vec3(0.5, 0.5, 0.5);\n                    vec3 c = vec3(2.0, 1.0, 0.0);\n                    vec3 d = vec3(0.5, 0.2, 0.25);\n                    return a + b * cos(6.28318 * (c * t + d));\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, int(u_complexity * 4.0 + 2.0));\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    uv /= (u_scale * 0.5 + 0.1 + u_bass * 0.05);\n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    // --- Layer 1: Bio-Digital (Xor) [Gyroid Lattice] ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * 1.;\n        \n            vec3 rd = normalize(vec3(layerUV, 1.0));\n            vec3 p = vec3(0.0);\n            float t = 0.0;\n            vec3 acc = vec3(0.0);\n            for(int i=0; i < int(float(32) * u_complexity); i++) {\n                p = rd * t;\n                p.z += localTime + u_bass; \n                p.xy = rotate(p.xy, localTime * 0.2);\n                float d = dot(sin(p), cos(p.yzx)) / 1.5;\n                d = abs(d) - 0.1;\n                if(d < 0.01) {\n                    acc += palette(t * 0.1 + u_mid * 0.5) * 0.1;\n                }\n                t += max(0.05, d * 0.5);\n            }\n            float intens = 1.0;\n            #ifdef u_intensity\n                intens = u_intensity;\n            #endif\n            vec3 layerColor = acc * intens;\n            \n        layerColor *= u_amplitude * (0.8 + u_mid * 0.4);\n        finalColor = layerColor;\n    }\n\n    // --- Layer 4: Particle System [Particles] ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.5) * 0.01 * (.10 + u_bass);\n        vec2 layerUV = currentUV;\n        float localTime = u_time * 1.69;\n        \n            vec3 rayDir = normalize(vec3(layerUV, -1.0));\n            vec3 p = vec3(0.0);\n            vec3 col = vec3(0.0);\n            float z = 0.0;\n            float d = 0.0;\n            \n            // Audio reactive time \n            float t = localTime * (1.5 * u_intensity) + u_treble * (2.0 * (0.8 + u_treble * 0.5));\n\n            for(float i = 1.0; i <= 60.0; i++) {\n                // 'f' defines a structural bounding depth scalar\n                float f = abs(z - 5.0) * (0.05 * u_scale);\n                \n                // The original code packed a float and a vec3 into a vec4 just to get the length.\n                // Unpacking it here is cleaner, prevents alpha-channel issues, and performs better:\n                float a = dot(sin(p), sin(p / 0.6)) - length(p) + 5.0;\n                vec3 b = sin(p / 0.1 + t) * cos(p.yzx / 0.1 + t) * 0.3;\n                \n                // Calculate equivalent distance safely\n                float dist = sqrt(a * a + dot(b, b));\n                \n                d = (0.4 * u_intensity) * max(f, dist - f);\n                z += d;\n                \n                // Color palette (d / 0.2 is replaced with d * 5.0 for multiplication speed)\n                vec3 pal = cos(p.y + i * 0.2 + d * 5.0 + vec3(0.0, 1.0, (2.0 * (0.8 + u_treble * 0.5)))) + (1.1 * u_complexity);\n                \n                // SAFE: Prevent divide-by-zero causing black pixels or NaN artifacts\n                col += pal / max(0.001, d);\n                \n                // Advance ray position for the NEXT iteration \n                // (This perfectly mimics Xor's third argument in the golfed for-loop)\n                p = z * rayDir;\n                p.z += (9.0 * u_intensity); \n            }\n            \n            float intens = 1.0;\n            #ifdef u_intensity\n                intens = u_intensity;\n            #endif\n            \n            // Original used tanh(o*o/3e5)\n            // We scale it dynamically based on the u_intensity and u_treble audio\n            vec3 layerColor = tanh((col * col / 300000.0) * intens * (1.0 + u_treble * 0.2));\n            \n        layerColor *= u_amplitude * (0.8 + u_treble * 0.4);\n        finalColor = blend_Screen(finalColor, layerColor, 0.5 + u_treble * 0.4);\n    }\n\n    // --- Layer 2: Plasma Waves [Liquid Noise] ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.5) * 0.01;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * 1.0;\n        \n            vec2 p = layerUV * (4.0 * (0.8 + u_treble * 0.01));\n            float n = noise(p + localTime * (0.5 * (0.4 + u_treble * 0.01)));\n            n += noise(p * 2.0 - localTime) * 0.5;\n            float ring = sin(n * (10.0 * u_scale) + localTime);\n            vec3 layerColor = palette(n + u_treble * 0.1) * (0.5 + (0.5 * (0.48 + u_treble * 0.1)) * ring);\n            \n        layerColor *= u_amplitude * (0.8 + u_treble * 0.4);\n        finalColor = blend_Add(finalColor, layerColor, 0.5 + u_treble * 0.2);\n    }\n\n    // --- Layer 3: Fractal Patterns [Sierpinski Gasket] ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.5) * 0.01 * (.10 + u_bass);\n        vec2 layerUV = currentUV;\n        float localTime = u_time * 0.76;\n        \n            vec2 p = layerUV * 2.0;\n            p += vec2(sin(localTime * 0.3), cos(localTime * 0.4)) * 0.1;\n            \n            vec3 col = vec3(0.0);\n            float scale = 1.0;\n            \n            // Iterated function system for Sierpinski\n            for(int i=0; i < int(float(8) * u_complexity); i++) {\n                // Fold space\n                p = abs(p);\n                \n                // Rotate and scale\n                float angle = localTime * 0.1 + float(i) * 0.2;\n                p = rotate(p, angle);\n                \n                // Scale\n                p = p * 2.0 - vec2(1.0, 0.0);\n                \n                // Accumulate color\n                vec3 pal = cos(p.x + vec3(1.0, 2.0, (3.0 * u_complexity)) + localTime * (0.2 * (0.8 + u_bass * 0.5))) + 1.0;\n                col += pal * scale * 0.1;\n                \n                scale *= 0.5;\n            }\n            \n            // Audio reactivity\n            col *= (0.5 + u_bass * 0.5);\n            vec3 layerColor = tanh(col * 0.5);\n            \n        layerColor *= u_amplitude * (0.8 + u_bass * 0.4);\n        finalColor = blend_Overlay(finalColor, layerColor, 0.5 + u_bass * 0.2);\n    }\n\n    \n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 + u_time) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = 0.95; \n    #ifdef u_decay\n        dcAmount = u_decay;\n    #endif\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = 0.0;\n    #ifdef u_feedback\n        fbAmount = u_feedback;\n    #endif\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.7) {\n        // Force feedback ON during high energy\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.778Z"
    },
    {
      "index": 17,
      "name": "Watership",
      "thumbnail": "data:image/jpeg;base64,",
      "params": {
        "intensity": 1,
        "scale": 1,
        "complexity": 0.9
      },
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\nuniform float u_intensity;\nuniform float u_scale;\nuniform float u_complexity;\n\n// Defaults for missing sliders\n#define u_speed 1.0\n#define u_amplitude 1.0\n#define u_glow 1.0\n#define u_colorShift 0.0\n#define u_distortion 0.0\n#define u_rotation 0.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n#define u_feedback 0.0\n#define u_decay 0.95\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            float freq = clamp(f, 0.01, 0.99);\n            float freqValue = texture(u_spectrum, vec2(freq, 0.0)).r;\n            \n            // Fallback: create demo pattern when no audio (spectrum is all zeros)\n            // This creates a gentle wave pattern so effects are visible in preview\n            float demo = 0.3 + 0.2 * sin(freq * 10.0 + u_time * 2.0) \n                             + 0.15 * sin(freq * 25.0 + u_time * 3.0);\n            \n            // Mix between real audio and demo based on audio level\n            float audioLevel = freqValue;\n            return mix(demo, freqValue, step(0.01, audioLevel));\n        }\n        \n\n                vec3 palette(float t) {\n                    return mix(vec3(1.0, 0.0, 0.0), vec3(1.0, 1.0, 0.0), t);\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, int(u_complexity * 4.0 + 2.0));\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    uv /= (u_scale * 0.5 + 0.1 + u_bass * 0.05);\n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    // --- Layer 1: Plasma Waves [Cellular] ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * 1.50;\n        \n            vec2 p = layerUV * 3.0;\n            vec2 i_st = floor(p);\n            vec2 f_st = fract(p);\n            float m_dist = 1.0;\n            for (int y= -1; y <= 1; y++) {\n                for (int x= -1; x <= 1; x++) {\n                    vec2 neighbor = vec2(float(x),float(y));\n                    vec2 point = vec2(random(i_st + neighbor), random(i_st + neighbor + 1.0));\n                    point = (0.5 * (0.8 + u_bass * 0.5)) + 0.5*sin(localTime + (6.2831 * u_complexity)*point);\n                    vec2 diff = neighbor + point - f_st;\n                    float dist = length(diff);\n                    m_dist = min(m_dist, dist);\n                }\n            }\n            vec3 layerColor = palette(m_dist + u_bass);\n            layerColor += 1.0 - step(0.02, m_dist); \n            \n        layerColor *= u_amplitude * (0.8 + u_bass * 0.4);\n        finalColor = layerColor;\n    }\n\n    // --- Layer 2: Fluid Dynamics [Alien Oil] ---\n    {\n        vec2 layerUV = currentUV;\n        float localTime = u_time * 1.09;\n        \n            vec4 o = vec4(layerUV, 1.0, 1.0);\n            for(int i=0; i < int(float(9) * u_complexity); i++) {\n                vec4 s = sin(o * (3.0 * (0.8 + u_mid * 0.5)) - o.yxwz + localTime * 0.1);\n                o += s;\n            }\n            vec3 layerColor = 1.0 - exp(-o.xyz * 0.1 * (1.0 + u_mid));\n            \n        layerColor *= u_amplitude * (0.8 + u_mid * 0.4);\n        finalColor = blend_Exclusion(finalColor, layerColor, 0.5 + u_mid * 0.2);\n    }\n\n    // --- Layer 3: Spectrum Analyzer [Digital Bars] ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.5) * 0.1 * (1.0 + u_bass);\n        vec2 layerUV = currentUV;\n        float localTime = u_time * 1.15;\n        \n            float fIndex = abs(layerUV.x); \n            float val = getFreq(fIndex);\n            float barWidth = 0.05;\n            float barID = floor(fIndex / barWidth);\n            float qVal = getFreq(barID * barWidth + barWidth * 0.5);\n            float bar = step(abs(layerUV.y), qVal * 0.8);\n            float grid = step((0.002 * (0.8 + u_treble * 0.5)), abs(mod(layerUV.x, barWidth) - barWidth * (0.5 * u_intensity)));\n            bar *= grid;\n            vec3 layerColor = palette(barID * 0.1 + localTime) * bar;\n            layerColor += vec3((0.2 * u_complexity), 0.5, 1.0) * bar * u_treble;\n            \n        layerColor *= u_amplitude * (0.8 + u_treble * 0.4);\n        finalColor = blend_Overlay(finalColor, layerColor, 0.5 + u_treble * 0.2);\n    }\n\n    // --- Layer 4: Fractal Patterns [Newton Fractal] ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.5) * 0.1 * (1.0 + u_bass);\n        vec2 layerUV = currentUV;\n        float localTime = u_time * 1.23;\n        \n            vec2 p = layerUV * 3.0;\n            vec2 z = p;\n            float t = localTime * 0.2;\n            \n            // Newton's method for z^3 - 1 = 0\n            for(int i=0; i < int(float(12) * u_complexity); i++) {\n                vec2 z2 = z * z;\n                vec2 z3 = z2 * z;\n                // Derivative: 3*z^2\n                vec2 deriv = (3.0 * u_complexity) * z2;\n                // Newton iteration: z = z - f(z)/f'(z)\n                // f(z) = z^3 - 1, but we add rotation for variety\n                vec2 f = z3 - vec2(cos(t), sin(t)) * (0.5 * u_intensity);\n                z = z - f / (deriv + 0.001);\n            }\n            \n            // Color based on which root it converges to\n            float r = length(z - vec2(1.0, 0.0));\n            float g = length(z - vec2(-(0.5 * (0.8 + u_treble * 0.5)), (0.866 * u_scale)));\n            float b = length(z - vec2(-0.5, -0.866));\n            \n            vec3 col = vec3(1.0 / (r + 0.1), 1.0 / (g + 0.1), 1.0 / (b + (0.1 * u_intensity)));\n            col = tanh(col * 0.3);\n            \n            // Audio reactivity\n            vec3 layerColor = col * (0.8 + u_treble * (0.5 * u_intensity));\n            \n        layerColor *= u_amplitude * (0.8 + u_treble * 0.4);\n        finalColor = blend_SoftLight(finalColor, layerColor, 0.5 + u_treble * 0.2);\n    }\n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 + u_time) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = 0.95; \n    #ifdef u_decay\n        dcAmount = u_decay;\n    #endif\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = 0.0;\n    #ifdef u_feedback\n        fbAmount = u_feedback;\n    #endif\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.7) {\n        // Force feedback ON during high energy\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.778Z"
    },
    {
      "index": 18,
      "name": "Fade",
      "thumbnail": "data:image/jpeg;base64,",
      "params": {
        "speed": 0.6,
        "intensity": 1.1,
        "rotation": 0,
        "feedback": 0.99,
        "decay": 0.9
      },
      "fragmentShader": "#version 300 es\nprecision highp float;\n\nuniform float u_time;\nuniform vec2 u_resolution;\nuniform float u_bass;\nuniform float u_mid;\nuniform float u_treble;\nuniform sampler2D u_prev_frame;\nuniform sampler2D u_spectrum;\nuniform float u_speed;\nuniform float u_intensity;\nuniform float u_rotation;\nuniform float u_feedback;\nuniform float u_decay;\n\n// Defaults for missing sliders\n#define u_amplitude 1.0\n#define u_glow 1.0\n#define u_complexity 1.0\n#define u_colorShift 0.0\n#define u_distortion 0.0\n#define u_scale 1.0\n#define u_frequency 1.0\n#define u_symmetry 0.0\n#define u_turbulence 0.0\n\n\nout vec4 fragColor;\n\n\n        float random(vec2 st) {\n            return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n        }\n\n        float noise(vec2 p) {\n            vec2 i = floor(p);\n            vec2 f = fract(p);\n            vec2 u = f * f * (3.0 - 2.0 * f);\n            return mix(mix(random(i + vec2(0.0,0.0)), \n                          random(i + vec2(1.0,0.0)), u.x),\n                      mix(random(i + vec2(0.0,1.0)), \n                          random(i + vec2(1.0,1.0)), u.x), u.y);\n        }\n\n        vec2 rotate(vec2 p, float angle) {\n            float c = cos(angle);\n            float s = sin(angle);\n            return vec2(p.x * c - p.y * s, p.x * s + p.y * c);\n        }\n\n        // Frequency texture sampler helper\n        float getFreq(float f) {\n            // Clamp to avoid texture edge artifacts\n            float freq = clamp(f, 0.01, 0.99);\n            float freqValue = texture(u_spectrum, vec2(freq, 0.0)).r;\n            \n            // Fallback: create demo pattern when no audio (spectrum is all zeros)\n            // This creates a gentle wave pattern so effects are visible in preview\n            float demo = 0.3 + 0.2 * sin(freq * 10.0 + u_time * 2.0) \n                             + 0.15 * sin(freq * 25.0 + u_time * 3.0);\n            \n            // Mix between real audio and demo based on audio level\n            float audioLevel = freqValue;\n            return mix(demo, freqValue, step(0.01, audioLevel));\n        }\n        \n\n                vec3 palette(float t) {\n                    return 0.5 + 0.5 * cos(6.28318 * (t + vec3(0.0, 0.33, 0.67)));\n                }\n\n        vec3 blend_Add(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend, opacity);\n        }\n        vec3 blend_Screen(vec3 base, vec3 blend, float opacity) {\n            return mix(base, 1.0 - (1.0 - base) * (1.0 - blend), opacity);\n        }\n        vec3 blend_Overlay(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(2.0 * base * blend, 1.0 - 2.0 * (1.0 - base) * (1.0 - blend), step(0.5, base)), opacity);\n        }\n        vec3 blend_SoftLight(vec3 base, vec3 blend, float opacity) {\n            return mix(base, mix(\n                2.0 * base * blend + base * base * (1.0 - 2.0 * blend), \n                sqrt(base) * (2.0 * blend - 1.0) + 2.0 * base * (1.0 - blend), \n                step(0.5, blend)\n            ), opacity);\n        }\n        vec3 blend_Difference(vec3 base, vec3 blend, float opacity) {\n            return mix(base, abs(base - blend), opacity);\n        }\n        vec3 blend_Exclusion(vec3 base, vec3 blend, float opacity) {\n            return mix(base, base + blend - 2.0 * base * blend, opacity);\n        }\n         \n\n        vec3 applyGlobalEffects(vec3 color, vec2 uv) {\n            // Placeholder\n            return color;\n        }\n        \n        float fbm(vec2 p, int octaves) {\n            float value = 0.0;\n            float amplitude = 0.5;\n            for(int i = 0; i < octaves; i++) {\n                value += amplitude * noise(p);\n                p *= 2.0;\n                amplitude *= 0.5;\n            }\n            return value;\n        }\n        \n        float fbm(vec2 p) {\n            return fbm(p, 4);\n        }\n        \n\nvoid main() {\n    // 1. Base Coordinates\n    vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / u_resolution.y;\n    vec2 initialUV = uv; \n\n    // 2. Global Camera Transforms\n    uv = rotate(uv, u_rotation * u_time * 0.2);\n    \n    // Dynamic Param modifiers\n    \n\n    vec2 currentUV = uv; \n\n    vec3 finalColor = vec3(0.0);\n    vec3 layerColor = vec3(0.0);\n\n    // 3. Render Pipeline\n    \n    // --- Layer 1: Bio-Digital (Xor) [Hypnotic Spiral] ---\n    {\n        currentUV = uv;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.26;\n        \n            vec2 p = layerUV * 2.0;\n            // SAFE: Added 1e-6 to length(p) to prevent log(0) which is -Infinity\n            vec2 v = vec2(atan(p.y, p.x), log(length(p) + 1e-6)) / 0.2 + 4.0;\n            \n            vec4 col = vec4(0.0);\n            float t = localTime + u_mid; \n\n            for(float i=1.0; i<9.0; i++) {\n                v += sin(v.yx * i - vec2(t, i)) / i;\n                col += (sin(vec4(v.x, v.x, v.y, v.x) + i) + 1.0) * (v.y * v.y);\n            }\n            // SAFE: Add epsilon to avoid divide by zero if col is black\n            vec3 layerColor = tanh(vec3(4.0, 2.0, 1.0) / (col.rgb + 0.001));\n            \n        layerColor *= u_amplitude * (0.8 + u_mid * 0.4);\n        finalColor = layerColor;\n    }\n\n    // --- Layer 2: Particle System [Satellites] ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.5) * 0.01 ;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.85;\n        \n            vec2 p = layerUV * 10.0;\n            vec2 s = vec2(3.0, 2.0 - sin(localTime * 0.25));\n            vec3 acc = vec3(0.0);\n            float ca = cos(localTime * 0.25);\n            float sa = sin(localTime * 0.25);\n            mat2 m = mat2(ca, -sa, sa, ca);\n            for(float i=0.0; i<3.0; i++) {\n                m *= mat2((0.8 * (0.8 + u_treble * 0.1)), -0.6, 0.6, 0.8); \n                vec2 v = m * (p / s);\n                vec2 fv = fract(v) - 0.5;\n                float d = length(fv * m * s) - 0.2 * sin(length(v) - localTime) - 0.2;\n                float shape = smoothstep((0.1 * u_intensity), 0.0, d);\n                vec3 col = (cos(i + vec3(0.0, 0.6, 1.2)) + 1.0) * 0.5;\n                acc += shape * col * (1.0 - acc);\n            }\n            vec3 layerColor = acc * (1.0 + u_treble);\n            \n        layerColor *= u_amplitude * (0.8 + u_treble * 0.4);\n        finalColor = blend_Add(finalColor, layerColor, 0.5 + u_treble * 0.2);\n    }\n\n    // --- Layer 3: Fluid Dynamics [Vapor] ---\n    {\n        currentUV += (vec2(finalColor.r, finalColor.g) - 0.5) * 0.01 ;\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 1.34;\n        \n             vec3 p = vec3(0.0);\n             vec4 o = vec4(0.0);\n              vec3 rayDir = normalize(vec3(layerUV.x, -abs(layerUV.y), -1.0));\n             float z = 0.0;\n             float f = 0.0;\n             for(float i=0.0; i<60.0; i++) {\n                 p = z * rayDir;\n                 for(float j=1.0; j<5.0; j++) {\n                     p += sin(p.zxy * j + j - localTime) / j;\n                 }\n                 f = 0.01 + (0.1 * (0.8 + u_mid * 0.5)) * abs(dot(sin(p * (0.7 * (0.8 + u_mid * 0.5))), cos(p).yzx));\n                 z += f;\n                 o += sin(p.y - localTime + vec4((6.0 * u_rotation), 1.0, 2.0, (3.0 * (0.8 + u_mid * 0.5)))) * f + f;\n             }\n             vec3 layerColor = tanh(o.rgb * o.rgb / (100.0 * (0.8 + u_mid * 0.5))) * (0.5 + u_mid);\n             \n        layerColor *= u_amplitude * (0.8 + u_mid * 0.4);\n        finalColor = blend_Difference(finalColor, layerColor, 0.5 + u_mid * 0.2);\n    }\n\n    // --- Layer 4: Crystal Structures [Radial Facets] ---\n    {\n        vec2 layerUV = currentUV;\n        float localTime = u_time * u_speed * 0.97;\n        \n            vec2 p = layerUV;\n            float a = atan(p.y, p.x);\n            float r = length(p);\n            float c = cos(a * 6.0);\n            float col = smoothstep(0.1, (0.2 * u_intensity), abs(c - r * 4.0 + sin(u_treble)));\n            vec3 layerColor = palette(r * 2.0) * col;\n            \n        layerColor *= u_amplitude * (0.8 + u_treble * 0.4);\n        finalColor = blend_Overlay(finalColor, layerColor, 0.5 + u_treble * 0.2);\n    }\n\n\n    // 4. Post Processing\n    finalColor *= 1.0 + u_bass * 0.15;\n    finalColor += max(vec3(0.0), finalColor * finalColor) * u_glow * 0.5;\n    finalColor.rgb = mix(finalColor.rgb, finalColor.gbr, sin(u_colorShift * 2.0 + u_time) * 0.5 + 0.5);\n    finalColor *= u_intensity;\n\n    // 5. Feedback Trail (Fixed Box & Brightness)\n    \n    vec2 screenUV = gl_FragCoord.xy / u_resolution.xy;\n    \n    // 1. STATIONARY SAMPLING (Attached Trails)\n    // No zoom, just a tiny bit of bass-driven diffusion to smooth the light\n    vec2 diff = (vec2(random(screenUV + u_time), random(screenUV - u_time)) - 0.5) * 0.002 * u_bass;\n    vec3 prevColor = texture(u_prev_frame, screenUV + diff).rgb;\n\n    // 2. BORDER FADE (Fix Grey Box)\n    vec2 border = smoothstep(vec2(0.0), vec2(0.02), screenUV) * (1.0 - smoothstep(vec2(0.98), vec2(1.0), screenUV));\n    prevColor *= border.x * border.y;\n\n    // 3. DECAY\n    float dcAmount = u_decay;\n\n    // 4. FEEDBACK AMOUNT\n    float fbAmount = u_feedback;\n\n    // --- RESTORED TRIGGER LOGIC ---\n    // Automatically enable trails on high energy\n    float volume = (u_bass + u_mid + u_treble) / 3.0;\n    \n    if(volume > 0.9) {\n        // Force feedback ON during high energy\n        fbAmount = max(fbAmount, 0.85);\n        // Make trails last slightly longer during the hit\n        dcAmount = max(dcAmount, 0.96);\n    }\n\n    // Apply Decay\n    prevColor *= dcAmount;\n\n    // 5. BLEND (Max = Light Painting)\n    vec3 trails = max(finalColor, prevColor);\n    finalColor = mix(finalColor, trails, clamp(fbAmount, 0.0, 1.0));\n        \n\n    fragColor = vec4(max(vec3(0.0), finalColor), 1.0);\n}",
      "exportedAt": "2026-04-15T01:58:52.778Z"
    }
  ]
}